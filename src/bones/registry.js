// Generado por `npm run bones` (boneyard-js). Este archivo se sobrescribe en
// cada captura; el placeholder existe para que el build no falle antes de la
// primera corrida. Con el registro vacio, cada <Skeleton> cae a su `fallback`.
import { registerBones } from 'boneyard-js'

registerBones({})
